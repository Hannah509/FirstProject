# FirstProject
# an edit
this is my attempt to make an edit please work

## please work!

import random
import sys

def roll(min, max):
    while True:
        print(random.randint(min, max))
        print(random.randint(min, max))
        sys.exit("ta da!")

roll(1, 6)